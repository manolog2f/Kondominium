@model IEnumerable<Kondominium_Entities.DashClientesMorosos>

<!--<script>
    function ExportarClientesMorosos()
    {
        debugger;
        var form = document.getElementById("frmPartialInvo");
        if (form.getAttribute("enctype") === "multipart/form-data")
        {
            //loading();

            var xhr = new XMLHttpRequest();
            xhr.open(form.method, form.action);
            xhr.onreadystatechange = function () {
                if (xhr.readyState == 4 && xhr.status == 200)
                {
                    debugger;-->
@*var t = JSON.parse(xhr.response);
         //var t1 = JSON.parse(xhr.response);

    /////     //    var iduploadHeader = t['ClienteId'];
         var mensaje = t.mensaje.Mensaje;
         var idMensaje = t.mensaje.Codigo;

         $.get('@Url.Action("_Mensajes" )', { error: idMensaje, Mensaje: mensaje }).done(function (data)
         {
             $("UploadFileMensajes").html(data);
             mensajes();
         });*@

<!--}
            }
        }
        xhr.send(new FormData(form));
    }
</script>-->

<div class="table-responsive">
    <table class="table table-striped table-hover table-bordered" id="dataTable" width="100%" cellspacing="0">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ClienteId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FullName)
                </th>

                <th>
                    @Html.DisplayNameFor(model => model.SaldoActual)
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.ClienteId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FullName)
                    </td>

                    <td>
                        @Html.DisplayFor(modelItem => item.SaldoActual)
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
<div>
    <button onclick="exportTableToExcel('dataTable')" class="btn btn-info"><i class="fa fa-file-excel" aria-hidden="true"></i> Exportar Excel</button>

    @*@using (Ajax.BeginForm("ExportarClientesMorosos", "Home", new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "partsQuoUpd", LoadingElementId = "loading" }, new { @id = "frmPartialInvo", enctype = "multipart/form-data" }))
        {
            <button type="button" class="btn btn-info" onclick="ExportarClientesMorosos()">
                <i class="fa fa-file-excel" aria-hidden="true"></i>
                Exportar todos
            </button>
        }*@
</div>

<div id="UploadFileMensajes"></div>